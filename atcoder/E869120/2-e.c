#include <stdio.h>

int mai(void)
{
	int i, j, l;
	char S[100000];
	char retu[1000000][100000];

	scanf("%s", S);

	l = strlen(S);

	for (i = 0; i < count; ++i)
	{
		for (int j = 0; j < count; ++j)
		{
			strncpy(retu[i], S + j, i)
		}
	}
}