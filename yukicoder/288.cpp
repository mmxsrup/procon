#include <iostream>
#include <algorithm>
#include <vector>
#include <cstdio>
using namespace std;
typedef long long ll;	
#define rep(i,n) for(int i=0;i<(n);i++)

int main(void){
	int n, ll m;
	cin >> n >> m;
	vector<int> a(n);
	rep(i, n) cin >> a[i];
	vector<ll> k(n);
	rep(i, n) cin >> k[i];



}